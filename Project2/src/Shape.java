
public abstract class Shape {
	/*
	 * concrete methods: which contains the implementation
	 * abstract methods: which contains just delcaration and the 
	 * 						implementation is left for the subclass
	 */
	public Shape() {
		
	}
	public abstract double calculateArea();
	
	/* 1. abstract class can contain all the methods or none of the methods abstract
	 * 2. abstract class can have constructor
	 * 
	 * 
	 */
}
